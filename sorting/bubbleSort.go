package sorting

/*
Сложность: O(n²)
🔍 Алгоритм Bubble Sort (Пузырьковая сортировка)
📌 Как работает?
Bubble Sort — это простой алгоритм, который проходит по массиву и постепенно "выталкивает" наибольшие элементы в конец, сравнивая и меняя местами соседние элементы.

Проход по массиву: сравниваем два соседних элемента.
Обмен: если левый больше правого — меняем местами.
Повторение: двигаемся дальше, пока не дойдем до конца массива.
Каждый проход "выставляет" наибольший элемент в конец.
Оптимизация: если в одном проходе не было перестановок — массив уже отсортирован, можно выйти раньше.
✨ Пример работы на массиве [5, 3, 8, 4, 2]
Изначально:
👉 [5, 3, 8, 4, 2]

Итерация 1: Проходим по массиву:

5 > 3 → меняем → [3, 5, 8, 4, 2]
5 < 8 → не меняем
8 > 4 → меняем → [3, 5, 4, 8, 2]
8 > 2 → меняем → [3, 5, 4, 2, 8] (самый большой элемент "всплыл" в конец)
Итерация 2: Проходим без 8:
👉 [3, 5, 4, 2, 8] → [3, 4, 5, 2, 8] → [3, 4, 2, 5, 8]

Итерация 3: Проходим без 5, 8:
👉 [3, 4, 2, 5, 8] → [3, 2, 4, 5, 8]

Итерация 4: Последний проход, 2 встанет на место:
👉 [2, 3, 4, 5, 8]

Массив отсортирован! 🎉
*/

func BubbleSort(arr []int) { // arr := []int{5, 3, 8, 4, 2}
	n := len(arr)
	for i := 0; i < n-1; i++ {
		swapped := false
		for j := 0; j < n-i-1; j++ {
			if arr[j] > arr[j+1] {
				arr[j], arr[j+1] = arr[j+1], arr[j]
				swapped = true
			}

		}
		if swapped == false {
			break
		}
	}

}
